[package]
name = "atlas-cli"
version = "0.2.0"
edition = "2024"
description = "Control the atlas homelab server: connect, boot, shutdown, restart"

[[bin]]
name = "atlas"
path = "src/main.rs"

[profile.release]
lto = true
strip = true
