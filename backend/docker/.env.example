# Copy to .env (gitignored) before `docker compose up`.
# POSTGRES_PASSWORD is the only variable compose.yml expects — database name
# and user are fixed to "atlas". Generate one: openssl rand -base64 24
POSTGRES_PASSWORD=change-me
